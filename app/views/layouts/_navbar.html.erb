<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Blog</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to 'My Posts', posts_path, class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to 'Create New Post', new_post_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
    <ul class="navbar-nav pull-right">
      <% if !user_signed_in? %>
        <li class="nav-item mr-2">          
            <%= link_to 'Sign up', new_user_registration_path,class: "btn btn-outline-primary" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign in', new_user_session_path,class: "btn btn-outline-primary" %>
        </li>
      <% else %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.username %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to 'Edit Accout', edit_user_registration_path(current_user.id), class: "dropdown-item" %>
            <%= link_to 'Sign Out', destroy_user_session_path, class: "dropdown-item",method: :delete %>
          </div>
        </li>
        <li class="nav-item">
          <%= image_tag(current_user.avatar.url(:thumb), width:"45", height:"45", class: "rounded-circle", alt: "No Image") %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>